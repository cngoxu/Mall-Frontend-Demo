<style>
    /* 余额卡片样式 */
    .balance-card {
        border-radius: 8px;
        padding: 20px;
    }

    /* 流水记录样式 */
    .collection .collection-item.avatar {
        padding-left: 80px;
        min-height: 80px;
    }

    .collection .collection-item.avatar .circle {
        width: 50px;
        height: 50px;
        line-height: 50px;
        font-size: 24px;
    }

    .collection .collection-item.avatar .title {
        font-weight: bold;
        font-size: 1.1rem;
    }

    .collection .collection-item.avatar .secondary-content {
        font-size: 1.2rem;
        font-weight: bold;
        margin-top: 10px;
    }

    /* 充值选项样式 */
    .recharge-option {
        padding: 15px;
        border: 1px solid #e0e0e0;
        border-radius: 4px;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s;
    }

    .recharge-option:hover {
        border-color: #009688;
        background-color: #e0f2f1;
    }

    .recharge-option input[type="radio"]:checked+span {
        color: #009688;
        font-weight: bold;
    }

    /* 响应式调整 */
    @media only screen and (max-width: 600px) {
        .collection .collection-item.avatar .secondary-content {
            float: none;
            display: block;
            margin-top: 5px;
            margin-left: 60px;
        }
    }
</style>